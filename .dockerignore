# Build context for infra/Dockerfile — only what the build stage needs.
*
!package.json
!package-lock.json
!tsconfig.json
!scripts/
!src/
!ui/
!infra/dev/pkg/*.tgz
# never send these even if a rule above matched a parent
**/node_modules
**/.DS_Store
